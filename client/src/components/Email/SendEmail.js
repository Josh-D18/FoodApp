import axios from "axios";

export default function SendEmail() {
  const SendEmailFunc = () => {};

  return (
    <button onClick={() => SendEmailFunc()}>
      Send Your Shopping Cart Ingredients To Your Email!
    </button>
  );
}
